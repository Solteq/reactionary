{
  "name": "angular-trpc",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "deployments/angular-trpc/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "platform": "node",
        "outputPath": "dist/deployments/angular-trpc",
        "format": ["cjs"],
        "bundle": false,
        "main": "deployments/angular-trpc/src/main.ts",
        "tsConfig": "deployments/angular-trpc/tsconfig.app.json",
        "assets": ["deployments/angular-trpc/src/assets"],
        "generatePackageJson": true,
        "esbuildOptions": {
          "sourcemap": true,
          "outExtension": {
            ".js": ".js"
          }
        }
      },
      "configurations": {
        "development": {},
        "production": {
          "esbuildOptions": {
            "sourcemap": false,
            "outExtension": {
              ".js": ".js"
            }
          }
        }
      }
    },
    "docker-build": {
      "dependsOn": [
        "build",
        { "projects": ["examples-angular"], "target": "build" }
      ],
      "command": "docker build -f deployments/angular-trpc/Dockerfile . -t registry.fly.io/angular-trpc --platform linux/amd64"
    },
    "docker-push": {
      "dependsOn": ["docker-build"],
      "command": "docker push registry.fly.io/angular-trpc:latest"
    },
    "deploy": {
      "dependsOn": ["docker-push"],
      "command": "flyctl deploy deployments/angular-trpc/"
    }
  }
}
