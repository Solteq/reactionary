import { createInitialRequestContext } from "../initialization.js";

describe('Analytics', () => {
  it('should be able to pass succesfully through the multicast provider', async () => {
    const context = createInitialRequestContext();
  });
});